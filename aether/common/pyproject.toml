[project]
name = "aether-common"
version = "0.1.0"
description = "Shared data models and utilities for Aether Drones"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
